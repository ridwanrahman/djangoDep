[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "djangoDep"
version = "1.0"
dependencies = [
    "Django==5.2.3",
    "python-dotenv",
    "gunicorn"
]

[project]
dynamic = ["version"]

[tool.setuptools_scm]
version_scheme = "post-release"